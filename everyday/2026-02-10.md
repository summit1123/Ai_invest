# Daily Log - 2026-02-10 (Tue)

## 오늘 완료
- 실행 자동화 파일 추가
  - `.gitignore`
  - `sql/minimal_schema.sql`
  - `scripts/check_env.sh`
  - `scripts/init_minimal_schema.py`
  - `scripts/smoke_tests.py`
  - `scripts/check_postgres_local.py`
  - `scripts/gh_login.sh`
  - `scripts/tool_status.sh`
  - `scripts/git_bootstrap.sh`
- Git 저장소 초기화
  - `git init`
  - 브랜치 `main` 고정
  - `origin=https://github.com/summit1123/Ai_invest.git` 연결
  - 로컬 초기 커밋 완료: `70104a0`
- 도구 상태 확인
  - `gh` 로컬 설치 완료 (`~/.local/bin/gh`)
  - 로컬 Postgres 점검 스크립트 준비 완료
- 운영 기준 변경
  - Docker 경로 폐기, 로컬 Postgres(`localhost:5432`) 기준으로 고정
- 검증 실행
  - Telegram 한국어 전송 성공
  - 단위 테스트 통과: `10 passed`

## 이슈/리스크
- 로컬 Postgres 미구동
  - `localhost:5432` 연결 실패 상태
- GitHub 인증 미완료
  - fetch/push 실패(인증 필요)
- Upbit 키 미입력
  - `UPBIT_ACCESS_KEY`, `UPBIT_SECRET_KEY` 누락
- DB 미구동으로 스키마 적용/DB 스모크 미완료
- 스모크 테스트 결과
  - Telegram: 성공
  - DB: 실패(5432 미구동)
  - Upbit: 실패(키 누락)

## 내일 계획 (우선순위)
1. 로컬 Postgres 서비스 실행 확인 (`localhost:5432`)
2. `python3 scripts/check_postgres_local.py`로 연결/pgvector 상태 점검
3. `python3 scripts/init_minimal_schema.py`로 최소 스키마 적용
4. `~/.local/bin/gh auth login` 실행 후 `scripts/git_bootstrap.sh` 재실행
5. `UPBIT_ACCESS_KEY`, `UPBIT_SECRET_KEY` 입력 후 `python3 scripts/smoke_tests.py` 재실행

## 체크포인트
- [x] Git 저장소/원격 연결
- [x] Telegram 전송 성공
- [x] 단위 테스트 성공
- [ ] 로컬 Postgres 실행
- [ ] DB 스키마 적용
- [ ] Upbit 인증 스모크 성공
- [ ] GitHub fetch/push 성공
