# Daily Log - 2026-02-10 (Tue)

## 오늘 완료
- 실행 자동화 파일 추가
  - `.gitignore`
  - `sql/minimal_schema.sql`
  - `scripts/check_env.sh`
  - `scripts/init_minimal_schema.py`
  - `scripts/smoke_tests.py`
  - `scripts/check_postgres_local.py`
  - `scripts/gh_login.sh`
  - `scripts/tool_status.sh`
  - `scripts/git_bootstrap.sh`
- Git 저장소 초기화
  - `git init`
  - 브랜치 `main` 고정
  - `origin=https://github.com/summit1123/Ai_invest.git` 연결
  - 로컬 초기 커밋 완료: `70104a0`
- 도구 상태 확인
  - `gh` 로컬 설치 완료 (`~/.local/bin/gh`)
  - 로컬 Postgres 점검 스크립트 준비 완료
- 운영 기준 변경
  - Docker 경로 폐기, 로컬 Postgres(`localhost:5432`) 기준으로 고정
- 검증 실행
  - Telegram 한국어 전송 성공
  - 단위 테스트 통과: `10 passed`

## 이슈/리스크
- (해결) 로컬 Postgres 연결 OK + pgvector 확인
- (해결) schema_v1_1 적용 완료
- (해결) DB/Telegram/Upbit 스모크 테스트 통과
- GitHub 인증/원격 push는 환경에 따라 추가 설정 필요(필수 아님)
- `.env`에 실키가 들어있으므로 커밋/공유 금지(이미 `.gitignore`)

## 내일 계획 (우선순위)
1. paper/governance/research 루프를 동시에 기동해 UI/텔레그램/DB e2e 검증
2. backtest v0 설계 확정 및 첫 리플레이 러너 추가
3. RULE_PATCH 제안/검증 파이프라인 설계(문서+스키마) 고정

## 체크포인트
- [x] Git 저장소/원격 연결
- [x] Telegram 전송 성공
- [x] 단위 테스트 성공
- [x] 로컬 Postgres 실행
- [x] DB 스키마 적용
- [x] Upbit 인증 스모크 성공
- [ ] GitHub fetch/push 성공

---

## 추가 완료(2차, v1.1 확장)
- 거버넌스 루프 + Trade Plan
  - `scripts/run_governance_loop.py` 추가(정시 회의 → `TRADE_PLAN_SET` 이벤트 생성)
  - `paper_loop`가 최신 Trade Plan을 읽어 심볼/비중 컨텍스트로 반영
  - UI/Backend: `GET /api/v1/ui/trade-plan/latest`, 프론트 `/trade-plan` 페이지 추가
- Telegram 가독성 개선(한국어)
  - reason_code → 한국어 타이틀 매핑(`ai_invest/domain/reason_codes_ko.py`)
  - Safe 결정 템플릿 개선: 스프레드/RSI/ATR/VolZ + 에이전트 요약 포함
- 비서(Secretary) Agent 도입(LLM optional)
  - OpenAI HTTP 클라이언트(`ai_invest/llm/openai_http.py`)
  - 회의 요약을 “사람이 읽는 회의록”으로 생성해 텔레그램 전송
  - 스모크 스크립트: `scripts/llm_smoke_test.py`
- 리서치 Agent(뉴스+시장) 분리(LLM optional)
  - RSS 기반 뉴스 수집(`ai_invest/research/rss.py`)
  - Research Agent가 한국어 브리프(JSON 출력) 생성(`ai_invest/agents/research_agent.py`)
  - `scripts/research_daily_brief.py`가 뉴스+시장 브리프 생성/저장/알림
- 테스트 보강
  - OpenAI client / Secretary / Research Agent 단위 테스트 추가
  - 현재 `pytest` 전체 통과

## 현재 상태(정리)
- 실시간 매매 판단: 결정적(LLM 비의존) 루프
  - Market/Regime/Risk/Ops → Safe Judge → PaperExecutor
- 거버넌스/리서치/회의록: LLM 사용 가능(실패 시 deterministic fallback)

## 다음 작업 계획(멀티데이)
1. E2E 운영 모드 고정(오늘)
  - 백엔드/프론트/루프 실행 스크립트 1개로 묶기(`scripts/run_dev_console.sh` 같은 형태)
  - paper/governance/research 루프를 동시에 돌려 UI/텔레그램/DB가 모두 채워지는지 검증
2. Backtest v0(1~2일)
  - Upbit 과거 캔들 다운로드 → 동일 피처/에이전트/세이프 젖지로 리플레이
  - 슬리피지/수수료/스프레드 비용 모델 반영(단순 모델부터)
  - 결과를 `runs(run_type=BACKTEST)` + 요약 리포트 저장
3. Rule Patch Proposer/Validator 루프(2~4일)
  - LLM이 `RULE_PATCH` JSON만 제안(코드 직접 변경 금지)
  - Validator가 스키마/범위/금지경로 검사 + 백테스트 통과 시 `RULE_APPROVED`
4. “트리거 → 즉시 처리 / 다음 회의 안건” 연결(상시)
  - 실시간 트리거는 즉시 Safe로 처리하고, 별도 이벤트/카드로 회의 안건에 누적
