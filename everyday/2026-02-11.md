# Daily Log - 2026-02-11 (Wed)

## 오늘 완료
- Prompt Contract V1 도입
  - 공통 프롬프트 계약 모듈 추가: `ai_invest/agents/prompt_contract.py`
  - 고정 섹션: `ROLE / OBJECTIVE / INPUT_CONTRACT / HARD_RULES / OUTPUT_SCHEMA / FAILSAFE`
- 거버넌스 멀티에이전트 프롬프트 정규화
  - 적용 대상: research/quant/risk/ops/critique/coordinator/secretary
  - 적용 파일: `ai_invest/meetings/governance_meeting.py`
- LLM 에이전트 프롬프트 정규화
  - `research_agent_daily_brief`: `ai_invest/agents/research_agent.py`
  - `strategy_coordinator`(trade plan/weekly): `ai_invest/agents/strategy_coordinator_agent.py`
  - `secretary_agent`(회의록): `ai_invest/agents/secretary_agent.py`
- 테스트 보강
  - 신규: `tests/test_prompt_contract.py`
  - 계약 섹션 누락/핵심 규칙 누락 회귀 방지
- 에이전트 사전업무 루프 추가
  - 모듈: `ai_invest/work/agent_work_loop.py`
  - 스크립트: `scripts/run_agent_work_loop.py`
  - 출력: `agent_daily_reports` + `events(AGENT_WORK_REPORT)`
  - 대상: `research_agent`, `quant_strategist`, `risk_manager`, `ops_manager`
- 회의 오케스트레이션 연동
  - 회의 시작 전 사전업무 최신성 점검(`missing/stale/fresh`)
  - 부족 시 사전업무 루프 1회 자동 실행 후 재평가
  - 회의 메시지에 `PREWORK_STATUS` 기록
  - Fact Pack에 `prework_reports` / `prework_status` 포함
  - 설정: `rules.yaml > governance.prework_max_age_min`
- 회의 입장 게이트 강화
  - `rules.yaml > governance.require_prework_reports: true`
  - 사전업무 미제출/지연 상태면 회의를 fail-closed로 중단
- 사전업무 상태 가시화
  - API: `GET /api/v1/ui/work-reports/latest`
  - UI: `/prework` 페이지 추가(에이전트별 정상/지연/미제출)
- 통합 실행 스크립트 추가
  - `scripts/run_multi_orchestrator.py`
  - paper/work/governance 루프를 한 번에 기동/감시(재기동 포함)
  - `--dry-run`으로 실행 커맨드 사전 확인 가능
- 운영자 관찰형 알림 설계/적용
  - 문서: `observer_mode_notifications.md`
  - Safe/회의/리서치 알림을 한국어 중심으로 가독성 강화
  - 회의 결과 플랜 알림(`tpl_trade_plan_set`) 추가
- 자본 티어 정책 도입
  - 모듈: `ai_invest/config/capital_policy.py`
  - 회의/사전업무/실시간 루프에 공통 적용
  - Safe 알림에 `capital_tier`, `target_cap` 노출
  - 설정: `rules.yaml > governance.capital_policy`
- 페이퍼 초기자본 상향
  - `rules.yaml > paper.initial_cash_krw: 10,000,000`
  - 원장 KRW 잔고도 1천만원으로 보정
- 리뷰 자동화 루프 추가
  - 스크립트: `scripts/run_review_loop.py`
  - 일간(`tpl_daily_review`) / 주간(`tpl_weekly_review`) 알림 자동 전송
  - 수동 검증용 강제 옵션: `--force-daily`, `--force-weekly`
- 오케스트레이터 상태 가시화
  - 상태 파일: `runtime/orchestrator_status.json`
  - API: `GET /api/v1/ui/orchestrator/status`
  - 통합 오케스트레이터 worker 확장: `review_loop` 포함 4개
- 서버 부팅 시 자동 운용 연결
  - `app` lifespan에서 오케스트레이터 자동기동/종료 연동
  - 중복 실행 방지(기존 외부 orchestrator 감지 시 skip)
  - 상태 API에 `autostart(enabled/started_here/reason/log_path)` 메타 노출

## 검증
- `uv run pytest -q` 통과
  - 결과: `70 passed, 7 subtests passed`
- 스모크 테스트 통과
  - DB/Telegram/Upbit 모두 성공

## 현재 상태 요약
- 실시간 실행 경로는 결정적(Safe Judge) 유지
- 회의/리서치/코디네이터/비서 LLM 프롬프트는 동일 계약 포맷으로 통일
- 프롬프트 품질 회귀를 테스트로 차단
- 자본 규모에 따른 비중/쿨다운 상한이 회의 플랜과 실시간 실행에 동시 반영됨
- 운영자는 오케스트레이터 상태/API와 텔레그램 리뷰 알림으로 상태를 추적 가능

## 다음 작업
1. 백테스트/리플레이 자동 검증 루프 연결
2. `decision_outcomes` -> RULE_PATCH 제안/검증 자동화
3. 회의 UI 스트리밍(라운드/반박/충돌해결) 시각화 강화

## 커밋 로그(세분화)
1. `기능: 에이전트 사전업무 루프 추가`
2. `기능: 회의 시작 전 사전업무 상태 점검/갱신`
3. `기능: 회의 사전업무 보고 미충족 시 차단`
4. `기능: 사전업무 상태 조회 API/화면 추가`
5. `기능: 통합 멀티 오케스트레이터 실행 스크립트 추가`
6. `문서: 운영자 관찰형 알림 설계 및 구현 단계 정의`
7. `기능: 운영자 친화 알림 고도화 및 트레이드플랜 전송 추가`
8. `기능: 자본 티어 기반 비중/판단 정책 적용`
9. `설정: 페이퍼 초기자본 1000만원으로 상향`
10. `기능: 리뷰 자동알림 루프 및 오케스트레이터 상태 가시화`
11. `설정: 리뷰 루프 시간정책 및 운영 런북 동기화`
12. `기능: 리뷰 루프 수동 강제실행 옵션 추가`
13. `기능: 서버 부팅 시 오케스트레이터 자동기동`

## 추가 완료(아키텍처 잔여 14%)
- 스케줄 강제/복구
  - 회의 슬롯 캐치업 추가: 재시작 후 누락 슬롯(00/08/16) 자동 보강
  - 워커 루프 정시 정렬 실행(`--align-to-clock`) 기본 적용
  - 파일: `ai_invest/meetings/governance_meeting.py`, `scripts/run_agent_work_loop.py`, `rules.yaml`
- 정책 활성화 게이트(fail-closed)
  - 백테스트 임계 통과 시만 `TRADE_PLAN_SET`/`GOVERNANCE_POLICY_SET` 활성화
  - 미통과 시 `TRADE_PLAN_PROPOSED`/`GOVERNANCE_POLICY_PROPOSED` 기록 + `TRADE_PLAN_BLOCKED` 이벤트
  - 회의 실패 시 `MEETING_FAIL_CLOSED` 이벤트 기록
- UI/API 가시화 강화
  - `GET /api/v1/ui/governance/status` 추가(활성/제안 정책, 활성/제안 플랜, READY/완료 업무)
  - 회의실/회의목록/회의상세/Today 화면에 정책버전·활성화상태·업무배정 표시

## 추가 검증
- `uv run pytest -q` 통과
  - 결과: `84 passed, 7 subtests passed`
- `uv run python scripts/run_multi_orchestrator.py --dry-run` 확인
  - workers: `paper + research + quant + risk + ops + governance + review`

## 추가 커밋 로그(세분화)
14. `운영: 회의 슬롯 캐치업과 워커 정시 정렬 스케줄 적용`
15. `거버넌스: 백테스트 통과 기반 정책·플랜 활성화 게이트 추가`
16. `대시보드: 정책버전·활성화상태·업무배정 거버넌스 상태 노출`
